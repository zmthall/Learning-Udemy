#ifndef EXERCISESEC8_H
#define EXERCISESEC8_H

void Diplay(int*, size_t);

void SumArr(int*, size_t);

int MaxElem(int*, size_t);
int MinElem(int*, size_t);

void sortArr(int*, size_t);

int linearSearch(int*, size_t, int);

int binarySearch(int*, size_t, int);

std::pair<size_t, size_t> CountPosNeg(int*, size_t);

void DrawNumberSwuare(size_t);

void drawLeftTriangle(size_t);

void Display2DArr(int*, size_t, size_t);

int avgNum(int*, size_t);

#endif /* EXERCISESEC8_H */